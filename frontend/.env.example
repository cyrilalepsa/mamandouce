# MamanDouce frontend (Vite) — copier vers .env
# cp frontend/.env.example frontend/.env

# URL de l'API (requis). Prod NeriaCorp : Worker ou sous-domaine API.
VITE_BACKEND_URL=http://localhost:8000

# Cloudinary lecture publique uniquement (pas de secrets API ici).
# Laisser vide = images fœtus locales /assets/fetus/
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_FETUS_FOLDER=mamandouce/fetus
VITE_CLOUDINARY_TRANSFORMS=f_auto,q_auto
