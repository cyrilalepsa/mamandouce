{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["node"],
      "variables": {
        "NIXPACKS_NODE_VERSION": "22"
      },
      "phases": {
        "build": {
          "cmds": [
            "npm install",
            "npm run build"
          ]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "node scripts/spa-proxy.mjs",
    "restartPolicyType": "ON_FAILURE"
  }
}
